
/**
 * @export
 * @interface LiveEncoding
 */
export default interface LiveEncoding {
    /**
     * Stream key of the live encoder
     * @type {string}
     * @memberof LiveEncoding
     */
    streamKey: string;

    /**
     * IP address of the live encoder
     * @type {string}
     * @memberof LiveEncoding
     */
    encoderIp: string;

}
