/**
 * Sets the Motion Vector Prediction Mode.
 * @export
 * @enum {string}
 */
enum MvPredictionMode {
    NONE = 'NONE',
    SPATIAL = 'SPATIAL',
    TEMPORAL = 'TEMPORAL',
    AUTO = 'AUTO'
}

export default MvPredictionMode;

