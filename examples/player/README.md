# Bitmovin Javascript Player Examples

- run `yarn add bitmovin-javascript`
- copy one of the examples to your project
- replace the `<PLACEHOLDERS>` with your api key, etc.
- run the example with `node xx_example_name.js`
